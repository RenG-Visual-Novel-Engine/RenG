def programStart() {
    arr = OSFileNames("D:\program\Go\src\RenG\Blue\game")
    
    for (i = 0; i < len(arr); ++i) {
        if (arr[i][len(arr[i]) - 3] + arr[i][len(arr[i]) - 2] + arr[i][len(arr[i]) - 1] == "rgo") {
            print(arr[i])
        }
    }
    
    if (!IsExistDataStore("user")) {
        MakeDataStore("user")
    } else {
    }
}